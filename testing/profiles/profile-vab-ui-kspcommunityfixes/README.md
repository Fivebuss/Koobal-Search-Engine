# Profile — KSPCommunityFixes (main install)

**Exclusive group:** `vab-ui-organizer`

Includes `FasterEditorPartList` and other editor patches — **P0** Koobal Search Engine conflict risk.

## Apply

```powershell
.\apply-profile.ps1 -Profile profile-vab-ui-kspcommunityfixes
```
