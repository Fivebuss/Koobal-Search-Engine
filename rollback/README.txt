# Koobal Search Engine rollback artifacts

*(Formerly Part Search Suggest / PartSearchSuggest.)*

Each version lives in a subfolder (e.g. `v0.7.0/`) with historical `PartSearchSuggest.dll`, version file, and changelog snapshot.

Restore steps: `Source/PartSearchSuggest/ROLLBACK.md`
